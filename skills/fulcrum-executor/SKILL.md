---
name: fulcrum-executor
description: Enter Executor for an explicit implementation request.
---

Run `fulcrum enter executor --description "$ARGUMENTS" --json`, adding `--bead ID` only when supplied. Pass the request literally and follow the returned workspace, ownership, and finish instructions. Respect host Plan Mode and do not write when it forbids writes. Never treat degraded registration as an owned implementation.
