---
name: fulcrum-bootstrap
description: Bootstrap or resume the stock Codex Desktop Fulcrum control plane.
---

Discover the retained local `~/fulcrum` checkout, selected instance, authoritative
configuration, saved Codex projects, supported native tools, and model/effort
matrix. Run `fulcrum bootstrap` with a stable request UUID and the observed
`codex_root`, `native_tools`, and `model_support`. Never install or use an App Server
fallback.

For every returned action, call `action claim` before invoking the exact named
native tool once, then call `action result` with the actual result. Do not edit its
arguments, invent a retry ID, or retry an uncertain native effect. A newly created
standing task must call `register_standing` with its retained action marker before
other activity. Preserve the fixed titles `🧰 STEWARD 🧰`, `🧭 MARSHAL 🧭`, and
`🔮 VIZIER 🔮`; correct a normalized title only through a separately retained
native action.

Resume bootstrap with a new request UUID after returned actions or prerequisites
settle. Setup is complete only when all three identities, the source-following MCP
server, six trusted hooks, broker socket, supported models/tools, transcript and
accounting checks, exact Marshal heartbeat, and focused acceptance are recorded.
Pass an `acceptance` object with true values for `workspace_access`,
`hook_identity`, `transcript_lifecycle`, `usage_accounting`, `task_targeting`, and
`schedule_overlap` only after each check actually succeeds. Bootstrap first creates
the heartbeat paused, then returns a distinct activation action after acceptance;
admission opens only after that activation result succeeds. Keep
admission paused and report exact gaps otherwise. Explain that Desktop Stop does
not durably pause Fulcrum and that an initial MCP configuration change can require
one exceptional Desktop reconnect; ordinary source edits never require restart,
activation, installation, or remote publication.
