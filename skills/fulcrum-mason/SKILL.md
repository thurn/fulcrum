---
name: fulcrum-mason
description: Enter Mason for a bounded architecture and test investigation.
---

Run `fulcrum enter mason --description "$ARGUMENTS" --json`, adding `--bead ID` for existing work. Follow the returned instructions. During degraded registration, use only the packaged diagnostic guidance and report missing authority explicitly.
