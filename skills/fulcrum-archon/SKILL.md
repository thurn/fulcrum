---
name: archon
description: Locate and open the Python-controller-owned Archon task.
---

Run `fulcrum archon`. Present the returned native task link to the user. Do not
convert the current conversation into Archon, create a replacement, inspect the
fleet, or write operational state. If no current Archon exists, direct the user
to `./scripts/setup`.
